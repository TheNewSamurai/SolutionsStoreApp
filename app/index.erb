<div data-role="panel" data-id="menu" data-hash="crumbs" data-context="a#default" id="paneled"> <!-- ADDED IN ORDER TO MAKE AS LEFT MENU -->

  <div data-role="page" id="main" data-hash="false" data-url="<%= Rho::RhoConfig.start_path %>/">
  
    <div data-role="header" data-position="inline">
      <h1>SolutionsStore</h1>
      <% if SyncEngine::logged_in > 0 %>
      <a href="<%= url_for :controller => :Settings, :action => :do_sync %>" class="ui-btn-left" data-icon="refresh">
        Sync
      </a>
      <a href="<%= url_for :controller => :Settings, :action => :logout %>" class="ui-btn-right" data-icon="star">
  	    Logout
  	  </a>
      <% else %>
          <a href="<%= url_for :controller => :Settings, :action => :login %>" class="ui-btn-right" data-icon="star">Login</a>
      <% end %>
    </div>
  
    <div data-role="content">
      <ul data-role="listview">
        <li>Simple
          <ul>
            <li><a href="Product">Product Listing</a></li>
            <li><a href="Partner">Partner Listing</a></li>
          </ul>
        </li>
      </ul>
    </div>
    
    <div data-role="footer" data-id="ew-footer" data-position="fixed">
      <div data-role="navbar">
      <ul>
        <li>
          <a href="#" data-transition="slide-up">
            Main
          </a>
        </li>
        <li>
          <% if SyncEngine::logged_in > 0 %>
            <a href="<%= url_for :controller => :Settings, :action => :logout %>" data-transition="slide-up">
              Logout
            </a>
          <% else %>
            <a href="<%= url_for :controller => :Settings, :action => :login %>" data-transition="slide-up">
              Login
            </a>
          <% end %>
        </li>
      </ul>
    </div>
  
  </div>

</div>
